import { n as supabase, t as hasSupabaseConfig } from "./client-Zk1XYzmI.js";
import { n as Skeleton } from "./Skeleton-DRUH6KYf.js";
import { useEffect, useState } from "react";
import { Link } from "@tanstack/react-router";
import { jsx, jsxs } from "react/jsx-runtime";
import { useTranslation } from "react-i18next";
//#region src/routes/sellers.index.tsx?tsr-split=component
function ratingColor(r) {
	if (r <= 3) return "bg-rating-low text-white";
	if (r <= 6) return "bg-rating-mid text-black";
	return "bg-rating-high text-white";
}
function SellersIndex() {
	const { t } = useTranslation();
	const [rows, setRows] = useState(null);
	useEffect(() => {
		if (!hasSupabaseConfig()) {
			setRows([]);
			return;
		}
		supabase.from("sellers").select("id,slug,name,photo_url,best_for_brand,rating").order("rating", { ascending: false }).then(({ data }) => setRows(data ?? [])).catch(() => setRows([]));
	}, []);
	return /* @__PURE__ */ jsxs("div", {
		className: "mx-auto max-w-5xl px-6 py-16",
		children: [/* @__PURE__ */ jsx("h1", {
			className: "font-display text-4xl font-bold mb-10",
			children: t("sellers.title")
		}), !rows ? /* @__PURE__ */ jsx("div", {
			className: "grid md:grid-cols-3 gap-6",
			children: Array.from({ length: 3 }).map((_, i) => /* @__PURE__ */ jsx(Skeleton, { className: "h-56" }, i))
		}) : rows.length === 0 ? /* @__PURE__ */ jsx("p", {
			className: "text-muted-foreground",
			children: "No sellers yet."
		}) : /* @__PURE__ */ jsx("div", {
			className: "grid md:grid-cols-3 gap-6",
			children: rows.map((s) => /* @__PURE__ */ jsx(Link, {
				to: "/sellers/$slug",
				params: { slug: s.slug },
				className: "border rounded-xl p-5 hover:shadow-lg transition bg-card animate-float-in",
				children: /* @__PURE__ */ jsxs("div", {
					className: "flex items-center gap-4",
					children: [
						/* @__PURE__ */ jsx("div", {
							className: "h-16 w-16 rounded-full bg-muted overflow-hidden",
							children: s.photo_url && /* @__PURE__ */ jsx("img", {
								src: s.photo_url,
								alt: s.name,
								className: "w-full h-full object-cover"
							})
						}),
						/* @__PURE__ */ jsxs("div", {
							className: "flex-1",
							children: [/* @__PURE__ */ jsx("h3", {
								className: "font-display font-bold",
								children: s.name
							}), s.best_for_brand && /* @__PURE__ */ jsxs("p", {
								className: "text-xs text-muted-foreground",
								children: [
									t("sellers.best_for"),
									": ",
									s.best_for_brand
								]
							})]
						}),
						/* @__PURE__ */ jsxs("span", {
							className: `px-2 py-1 rounded text-xs font-bold ${ratingColor(s.rating)}`,
							children: [s.rating, "/10"]
						})
					]
				})
			}, s.id))
		})]
	});
}
//#endregion
export { SellersIndex as component };
